<p id="notice"><%= notice %></p>

<div class="center hero-unit">
  <h2>Invitation</h2>
  <h2>Welcome to Tearoomy</h2>
  
  
  <h5>Tearoomy allows you to create private message boards with your friends.</h5><br><br><br>
    
  
  <br>

<p>This invitation was generated by:</p> 

<p>
  <b>Sender:</b>
  <%= User.find_by_id(@invitation.sender_id).screenname %>
</p> 

<p>
  <b>Room:</b>
  <%= Room.find_by_id(@invitation.room_id).room_name %>
</p>



<% if signed_in? %>
  <% if @already_in_group %>
    You are already in this group
  <% else %>
    <%= link_to "Confirm", confirm_path(@invitation.token) %>
  <% end %>
<% else %>
  <%= link_to "Sign up and confirm", confirm_path(@invitation.token) %>
<% end %>

</div>





